#include<iostream>
using namespace std;

int main() {
	int n;
    cin>>n;
    int ans=0;
    while(n!=0){
       int digit = n%10;
        ans = ans+digit;
         if(ans!=0 && (n/10)!=0){
                ans = ans*10;
            }
        n =n/10;  
    }
    cout<<ans;
}
